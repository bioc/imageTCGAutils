% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matchHoverNetToTiles.R
\name{matchHoverNetToTiles}
\alias{matchHoverNetToTiles}
\title{Match HoverNet Nuclei to ProvGigaPath Tiles}
\usage{
matchHoverNetToTiles(
  hovernet,
  tiles,
  tile_size = 224,
  cell_x = "x_centroid",
  cell_y = "y_centroid",
  tile_x = "tile_x",
  tile_y = "tile_y",
  tile_id = "tile_id",
  cell_type = "type"
)
}
\arguments{
\item{hovernet}{A \code{SpatialExperiment} or \code{SpatialFeatureExperiment} object
containing HoverNet nuclei data with spatial coordinates and cell type
information.}

\item{tiles}{A \code{data.frame} or \code{tibble} containing ProvGigaPath tile data
with tile coordinates and embeddings.}

\item{tile_size}{Numeric. Size of tiles in pixels. Default is \code{224}.}

\item{cell_x}{Character. Name of the x-coordinate column in HoverNet data.
Default is \code{"x_centroid"} for h5ad data.}

\item{cell_y}{Character. Name of the y-coordinate column in HoverNet data.
Default is \code{"y_centroid"} for h5ad data.}

\item{tile_x}{Character. Name of the tile x-coordinate column in tiles data.
Default is \code{"tile_x"}.}

\item{tile_y}{Character. Name of the tile y-coordinate column in tiles data.
Default is \code{"tile_y"}.}

\item{tile_id}{Character. Name of the tile ID column in tiles data.
Default is \code{"tile_id"}.}

\item{cell_type}{Character. Name of the cell type column in HoverNet data.
Default is \code{"type"}.}
}
\value{
A list with three elements:
\describe{
\item{tiles_with_nuclei}{A \code{data.frame} with original tile data plus
cell type counts (\code{N}) and labels (\code{cell_type_label}) for each tile.}
\item{tiles_dominant}{A \code{data.frame} with tile IDs and their dominant
(most frequent) cell type.}
\item{scale_factor}{A list containing the computed scale factor, and
separate x and y scale factors.}
}
}
\description{
Assigns HoverNet nuclei to ProvGigaPath tiles by computing a
scale factor to align coordinate systems, then performing spatial matching.
Returns tile-level cell type counts and dominant cell types.
}
\details{
The function performs the following steps:
\enumerate{
\item Computes a scale factor to align nuclei coordinates with
tile coordinates
\item Scales nuclei coordinates using the computed scale factor
\item Creates bounding boxes for each tile based on tile_size
\item Assigns nuclei to tiles using spatial overlap
\item Counts cell types per tile
\item Identifies the dominant cell type for each tile
\item Merges results back to the original tiles data
}

The scale factor is computed as the mean of x and y scale factors, where
each is the ratio of coordinate ranges between nuclei and tiles.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(imageFeatureTCGA)
hov_file <- paste0(
    "https://store.cancerdatasci.org/hovernet/h5ad/",
    "TCGA-23-1021-01Z-00-DX1.F07C221B-D401-47A5-9519-10DE59CA1E9D.h5ad.gz"
)
hn_spe <- HoverNet(hov_file, outClass = "SpatialExperiment") |> import()

tile_prov_url <- paste0(
    "https://store.cancerdatasci.org/provgigapath/tile_level/",
    "TCGA-23-1021-01Z-00-DX1.F07C221B-D401-47A5-9519-10DE59CA1E9D.csv.gz"
)
pg_spe <- ProvGiga(tile_prov_url) |> import()

result         <- matchHoverNetToTiles(hn_spe, pg_spe)
tiles_matched  <- result$tiles_with_nuclei
dominant_types <- result$tiles_dominant
scale_info     <- result$scale_factor
\dontshow{\}) # examplesIf}
}
